{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build 雙向廣度優先搜尋",
      "type": "process",
      "command": "dotnet",
      "args": [
        "build",
        "${workspaceFolder}/雙向廣度優先搜尋/雙向廣度優先搜尋/雙向廣度優先搜尋.csproj",
        "--configuration",
        "Debug",
        "--nologo"
      ],
      "problemMatcher": "$msCompile",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
