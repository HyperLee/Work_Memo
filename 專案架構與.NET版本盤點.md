# Work_Memo 專案架構與 .NET 版本完整盤點

- 掃描日期：2026-09-15
- 掃描根目錄：/Users/qiuzili/WorkMemo/Work_Memo
- 執行環境：macOS arm64；.NET SDK 10.0.100；目前安裝 runtime 只有 Microsoft.NETCore.App 10.0.0

本報告把「專案檔結構完整」與「在目前 macOS 直接執行」分開統計。Windows-only、舊式 .NET Framework、缺少本機 runtime、測試案例失敗，都不會被誤算成缺少專案檔。

## 結論摘要

| 項目 | 數量 | 判定 |
|---|---:|---|
| 掃描到的最上層資料夾 | 97 | 全部遞迴檢查 |
| 有完整 .NET 專案檔的專案 | 89 | 找到 89 個 .csproj；宣告的來源、資源、專案參考與 HintPath 缺檔數為 0 |
| 方案檔 | 88 | 2 個方案容器異常，詳見下方 |
| 結構完整但目前 macOS 無法直接建置的舊式專案 | 34 | 目標為 .NET Framework v4.x；錯誤是缺少 Windows Developer Pack（MSB3644） |
| SDK-style 建置成功 | 54 | 50 個主控台 + 1 個測試專案 + 3 個 WinForms |
| SDK-style 建置失敗 | 1 | WinFormsApp_CreateLog_Fun：Form1.cs:17 的 CS1009 逸出序列錯誤 |
| 目前 macOS 依指定 TFM 直接執行成功 | 2 | net10.0 主控台：3乘5陣列取總和、BackTrack（送出 0 離開） |
| 沒有專案/方案檔的資料夾 | 9 | 排除，不列入專案統計 |

### 如何解讀「完整且可執行」

- 嚴格以目前主機的「指定 TFM 直接可執行」計算，2/89 個通過。
- 其餘有大量是環境限制：net8.0/net9.0 的主控台專案已成功建置，但目前沒有 .NET 8/9 runtime；用 DOTNET_ROLL_FORWARD=Major 可啟動 smoke run。
- 使用 Console.ReadKey 的範例在管線輸入下會因重導輸入而出現 InvalidOperationException；代表性 PTY 執行已成功，這不是缺檔。
- WinForms 專案需要 Windows Desktop runtime/Windows UI；舊式 .NET Framework 還需要 Windows Developer Pack，因此不列入本機 macOS 可執行數。

## 架構與 .NET 版本分組

下列清單列出每個被辨識到的 .csproj，路徑相對於掃描根目錄。

### SDK-style 現代 .NET 主控台 — net10.0（2 個）

可建置；兩個均已在目前 .NET 10 runtime 執行成功。

- 3乘5陣列取總和/3乘5陣列取總和/3乘5陣列取總和.csproj
- BackTrack/BackTrack/BackTrack.csproj — ⚠️ BackTrack.slnx 是空方案，直接 csproj 可建置

### SDK-style 現代 .NET 主控台 — net9.0（5 個）

可建置；目前沒有 .NET 9 runtime，直接 run 需先安裝 runtime 或使用 major roll-forward。

- 差分陣列/差分陣列/差分陣列.csproj
- DictionaryEquals用法/DictionaryEquals用法/DictionaryEquals用法.csproj
- PriorityQueue介紹/PriorityQueue介紹/PriorityQueue介紹.csproj
- 最上層陳述式必須在命名空間和型別宣告之前/Console/ConsoleApp1/ConsoleApp1.csproj
- 滑動視窗/SlidingWindow/SlidingWindow.csproj

### SDK-style 現代 .NET 主控台 — net8.0（43 個）

可建置；目前沒有 .NET 8 runtime，直接 run 需先安裝 runtime 或使用 major roll-forward。

- 二元樹輸出/二元樹輸出/二元樹輸出.csproj
- 二分法查找標準題/二分法查找標準題/二分法查找標準題.csproj
- 二維陣列中的查找/二維陣列中的查找/二維陣列中的查找.csproj
- 二維陣列輸出/二維陣列輸出/二維陣列輸出.csproj
- 平行處理-async 和 await練習/平行處理-async 和 await練習/平行處理-async 和 await練習.csproj
- 平行處理-Parallel練習/平行處理-Parallel練習/平行處理-Parallel練習.csproj
- 平行處理-Task練習/平行處理-Task練習/平行處理-Task練習.csproj
- 各型態大小/各型態大小/各型態大小.csproj
- 回文切割/回文切割/回文切割.csproj
- 多工處理-使用 async 和 await練習/多工處理-使用 async 和 await練習/多工處理-使用 async 和 await練習.csproj
- 多工處理-使用 Parallel 類練習/多工處理-使用 Parallel 類練習/多工處理-使用 Parallel 類練習.csproj
- 多工處理-使用 Task 類練習/多工處理-使用 Task 類練習/多工處理-使用 Task 類練習.csproj
- 多工處理-使用 Thread 類練習/多工處理-使用 Thread 類練習/多工處理-使用 Thread 類練習.csproj
- 多工處理-使用 ThreadPool練習/多工處理-使用 ThreadPool練習/多工處理-使用 ThreadPool練習.csproj
- 呼叫其他class/呼叫其他class/呼叫其他class.csproj
- 拓扑排序/拓扑排序/拓扑排序.csproj
- 拓樸排序_DFS/拓樸排序_DFS/拓樸排序_DFS.csproj
- 拓樸排序_Kahn's Algorithm/拓樸排序_Kahn's Algorithm/拓樸排序_Kahn's Algorithm.csproj
- 計算兩數總和且不使用加減法/計算兩數總和且不使用加減法/計算兩數總和且不使用加減法.csproj
- 計數排序/計數排序/計數排序.csproj
- 迴文範例1/迴文範例1/迴文範例1.csproj
- 陣列中重複的數字/陣列中重複的數字/陣列中重複的數字.csproj — ⚠️ .sln 的 csproj 相對路徑少了分隔符，直接 csproj 可建置
- 陣列計算最大_最小_平均值/陣列計算最大_最小_平均值/陣列計算最大_最小_平均值.csproj
- 動態規劃測試/動態規劃測試/動態規劃測試.csproj
- 參考傳回ref用法/參考傳回ref用法/參考傳回ref用法.csproj
- 插入排序/插入排序/插入排序.csproj
- 替換空格/替換空格/替換空格.csproj
- 進階排序string_CompareOrdinal/進階排序string_CompareOrdinal/進階排序string_CompareOrdinal.csproj
- 順時針_螺旋_輸出矩陣內容/順時針_螺旋_輸出矩陣內容/順時針_螺旋_輸出矩陣內容.csproj
- 雙向廣度優先搜尋/雙向廣度優先搜尋/雙向廣度優先搜尋.csproj
- Array_Sort進階用法/Array_Sort進階用法/Array_Sort進階用法.csproj
- Backtracking 回溯法/Backtracking 回溯法/Backtracking 回溯法.csproj
- BitwiseSubsequence/BitwiseSubsequence/BitwiseSubsequence.csproj
- Dic.FirstorDefault用法/Dic.FirstorDefault用法/Dic.FirstorDefault用法.csproj
- Dic.TryAdd方法示範/Dic.TryAdd方法示範/Dic.TryAdd方法示範.csproj
- Dictionary存陣列資料輸出/Dictionary存陣列資料輸出/Dictionary存陣列資料輸出.csproj
- Floyd-Warshall算法/Floyd-Warshall算法/Floyd-Warshall算法.csproj
- Grid題目類型解法/Grid題目類型解法/Grid題目類型解法.csproj
- list內含二維陣列輸出範例/list內含二維陣列輸出範例/list內含二維陣列輸出範例.csproj
- list排序/list排序/list排序.csproj
- Stack練習/Stack練習/Stack練習.csproj
- Tuple方法介紹/Tuple方法介紹/Tuple方法介紹.csproj
- XOR用法,判斷數字只出現一次/XOR用法,判斷數字只出現一次/XOR用法,判斷數字只出現一次.csproj

### SDK-style WinForms — net6.0-windows（1 個）

可建置；Windows-only，macOS 不執行 GUI。

- FlowLayoutPanelDemo/FlowLayoutPanelDemo/FlowLayoutPanelDemo.csproj

### SDK-style WinForms — net7.0-windows（2 個）

webview2_test 可建置；WinFormsApp_CreateLog_Fun 有 CS1009，兩者均 Windows-only。

- webview2_test/webview2_test/webview2_test.csproj — ✅ 含 Microsoft.Web.WebView2 1.0.1823.32
- WinFormsApp_CreateLog_Fun/WinFormsApp_CreateLog_Fun/WinFormsApp_CreateLog_Fun.csproj — ❌ 建置失敗：Form1.cs:17 CS1009

### SDK-style WinForms — net8.0-windows（1 個）

可建置；Windows-only，macOS 不執行 GUI。

- WinFormsApp1-測試dg按鈕/WinFormsApp1-測試dg按鈕/WinFormsApp1-測試dg按鈕.csproj

### SDK-style xUnit 測試專案 — net9.0（1 個）

可建置；執行結果 4/5 通過，1 個測試失敗，故不算全通過。

- 滑動視窗/SlidingWindow.Tests/SlidingWindow.Tests.csproj — ⚠️ 測試 4/5 通過，1 失敗

### 舊式 .NET Framework 主控台 — v4.8（25 個）

專案檔與來源結構完整；目前 macOS 建置缺 .NET Framework 4.8 Developer Pack。

- 二分法找mid寫法/二分法找mid寫法/二分法找mid寫法.csproj
- 不規則陣列/不規則陣列/不規則陣列.csproj
- 西元年轉民國年/西元年轉民國年/西元年轉民國年.csproj
- 身分證隱碼/身分證隱碼/身分證隱碼.csproj
- 前綴和 Prefix Sum/前綴和 Prefix Sum/前綴和 Prefix Sum.csproj
- 動態決定迴圈次數以及要跑資料/動態決定迴圈次數以及要跑資料/動態決定迴圈次數以及要跑資料.csproj
- 常見數學除法題目用法/常見數學除法題目用法/常見數學除法題目用法.csproj
- 亂數產生陣列做加總/亂數產生陣列做加總/亂數產生陣列做加總.csproj
- 資料隱碼中間三位/資料隱碼中間三位/資料隱碼中間三位.csproj
- 輸入整數排序/輸入整數排序/輸入整數排序.csproj
- 輸出符號/輸出符號/輸出符號.csproj
- 優化遞迴方式/優化遞迴方式/優化遞迴方式.csproj
- 隱碼-保留前六個字/隱碼-保留前六個字/隱碼-保留前六個字.csproj
- bebblesort/bebblesort/bebblesort.csproj
- C# OOP 多型/C# OOP 多型/C# OOP 多型.csproj
- C# OOP 抽象/C# OOP 抽象/C# OOP 抽象.csproj
- C# OOP 封裝/C# OOP 封裝/C# OOP 封裝.csproj
- C# OOP 繼承/C# OOP 繼承/C# OOP 繼承.csproj
- Fibonacci/Fibonacci/Fibonacci.csproj
- IList宣告與初始化方法/IList宣告與初始化方法/IList宣告與初始化方法.csproj
- List去除重複項目/List去除重複項目/List去除重複項目.csproj
- ListNode輸出範例/ListNode輸出範例/ListNode輸出範例.csproj
- OOP-class-1/OOP-class-1/OOP-class-1.csproj
- post-increment/post-increment/post-increment.csproj
- return array output/return array output/return array output.csproj

### 舊式 .NET Framework 主控台 — v4.5（6 個）

專案檔與來源結構完整；目前 macOS 建置缺 .NET Framework 4.5 Developer Pack。

- Json_reader/Json_reader/Json_reader.csproj — ✅ packages.config/Newtonsoft.Json 外部 DLL 存在
- linklist_Palindrome_cycle/linklist_Palindrome_cycle/linklist_Palindrome_cycle.csproj
- linklist_Palindrome_single/linklist_Palindrome_single/linklist_Palindrome_single.csproj
- linklist_Palindrome/linklist_Palindrome/linklist_Palindrome.csproj
- list_reverse/list_reverse/list_reverse.csproj
- Unique_List/Unique_List/Unique_List.csproj

### 舊式 .NET Framework 主控台 — v4.5.2（1 個）

專案檔與來源結構完整；目前 macOS 建置缺 .NET Framework 4.5.2 Developer Pack。

- Maximum even sum/Maximum even sum/Maximum even sum.csproj

### 舊式 .NET Framework WinForms — v4.5（1 個）

專案檔、Form、Designer、resx 與設定檔存在；目前 macOS 建置缺 .NET Framework 4.5 Developer Pack。

- DG互傳在觸發按鈕/DG互傳在觸發按鈕/DG互傳在觸發按鈕.csproj

### 舊式 .NET Framework WinForms — v4.5.2（1 個）

專案檔、Form、Designer、resx、packages.config 與外部 DLL 存在；目前 macOS 建置缺 .NET Framework 4.5.2 Developer Pack。

- TestOdsReader_P1/TestOdsReader_P1/TestOdsReader_P1.csproj — ✅ packages.config/DotNetZip 外部 DLL 存在

## 方案檔與專案完整度交叉檢查

直接以專案檔建置時，89 個 .csproj 的明確檔案參考均存在（缺檔 0）。但方案容器不是全部正確：

- BackTrack/BackTrack.slnx：沒有任何專案參考，內容是空方案。
- 陣列中重複的數字/陣列中重複的數字.sln：參考不存在的陣列中重複的數字陣列中重複的數字.csproj，路徑少了分隔符。

未被有效方案參考的實際專案檔（仍可直接以 dotnet build <path>.csproj 判定）：

- BackTrack/BackTrack/BackTrack.csproj
- 陣列中重複的數字/陣列中重複的數字/陣列中重複的數字.csproj

這兩項是方案檔問題，不是專案目錄缺少 .csproj 或來源檔；若要求「方案也必須可由 Visual Studio 直接載入」，需另行修正方案檔。

## 排除、不列入統計的資料夾

以下最上層資料夾沒有 .sln/.slnx/.csproj 或其他可辨識的完整專案入口，因此不列入上面的 89 個專案統計：

- AES加密範例 — 只有 AES加密範例/TestEncrypt.aspx、AES加密範例/TestEncrypt.aspx.cs
- ASP.NET匯出excel跨行合併 — 只有 ASP.NET匯出excel跨行合併/ASP.NET匯出excel跨行合併.txt
- SQL知識 — 只有 SQL 知識筆記 .sql 檔
- ___隨手筆記 — 只有 Markdown 筆記
- catch使用差異 — 只有 c# catch.MD
- c語言指標 — 只有 c語言指標.MD
- 單調棧 — 只有 單調棧.MD
- 白箱應對 — 只有 白箱調整.MD
- 輸出執行結果 — 只有 顯示執行結果1.MD

其中 AES加密範例 只有 ASP.NET .aspx/.aspx.cs，ASP.NET匯出excel跨行合併 只有文字筆記；它們不是可獨立建置的完整 ASP.NET 專案，故排除。

## 驗證紀錄

### 建置

對全部 .csproj 執行：

~~~text
dotnet build <project>.csproj --nologo --verbosity:minimal -p:EnableWindowsTargeting=true
~~~

結果：54 PASS / 35 FAIL / 89 total。35 個失敗中，34 個是舊式 .NET Framework 的 MSB3644，1 個是 WinFormsApp_CreateLog_Fun/Form1.cs:17 的 CS1009。

### 主控台執行

目前主機指定 TFM 的直接執行結果：

- 3乘5陣列取總和：net10.0，執行成功。
- BackTrack：net10.0，輸入 0 後正常顯示「再見！」並結束。
- net8.0 與 net9.0 主控台專案：建置成功，但直接 run 回報缺少 Microsoft.NETCore.App 8.0.0/9.0.0；這是本機 runtime 安裝狀態，不是專案缺檔。
- DOTNET_ROLL_FORWARD=Major smoke run：50 個現代 .NET 主控台中 27 個在管線輸入下正常結束；另外 23 個已輸出主要結果後停在程式刻意設定的 Console.ReadKey，管線重導會觸發輸入重導例外；代表性 PTY 執行已正常結束。

### 測試專案

執行 滑動視窗/SlidingWindow.Tests/SlidingWindow.Tests.csproj：4 passed, 1 failed, total 5。失敗案例是：

- 測試預期 25，實際為 24；測試資料 [1,4,2,10,2,3,1,0,20]、k=4 的最大連續視窗和確實是 24。此報告只記錄結果，不修改原始程式或測試。

## 名詞對照

- .NET Framework：本資料夾中的 v4.5、v4.5.2、v4.8 舊式 MSBuild 專案，主要是 Visual Studio Windows 專案。
- SDK-style 現代 .NET：本資料夾中的 net8.0、net9.0、net10.0；這些是現代 .NET，不是舊式 .NET Framework。
- WinForms：由 UseWindowsForms=true、-windows TFM、WinExe、System.Windows.Forms 與 Form Designer 檔案交叉判定。
- 本次沒有發現以 netcoreapp* 宣告的專案；若你說的「.netcode」是指 .NET Core，目前對應的是 SDK-style 現代 .NET 這一組。

---

## 工作區狀態

- tracked 的原始專案程式碼與設定檔沒有內容變更；本 Markdown 是本次新增的盤點結果。
- 建置／執行驗證留下 95 個未追蹤的 `bin/`／`obj/` 生成檔。依本資料夾 `AGENTS.md` 的限制，本次不執行批次刪除；若要清理，請由你手動移除這些生成檔。

本文件只新增盤點結果；原始專案程式碼未因盤點而修改。
